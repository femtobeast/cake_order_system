<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <meta name="google-signin-client_id"
        content="915969573855-vjr1qlcc9pnevsvp0gnlc4lg5jqk508o.apps.googleusercontent.com">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
    <script src="https://apis.google.com/js/platform.js" async defer></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Latest compiled and minified CSS -->


    <script>
        function onSignIn(googleUser) {
            var profile = googleUser.getBasicProfile();
            $(".g-sign2").css("display", "none");
            $(".data").css("display", "block");
            $("#pic").attr("src", profile.getImageUrl());
            $("#email").text(profile.getEmail());


        }

        function onSignOut() {
            var  auth2 = gapi.auth2.getAuthInstance();
            auth2.signOut().then(function(){
                alert("You have loged out!! Session Out")
                $(".g-sign2").css("display", "block");
            $(".data").css("display", "none");
            });


        }
        
        </script>
    <Style>
        .g-signin2 {
            margin-left: 500px;
            margin-top: 200px;
        }

        .data {
            display: none;
        }
    </Style>
    <title>Document</title>
</head>

<body>
        <h1><%= wt %></h1>
        <h2>My Favorite Things</h2>
        <ul>
          <% favoriteThings.forEach(function(thing){ %>
            <li><%= thing %></li>
          <% }) %>
        </ul>
    <div class="g-signin2" data-onsuccess="onSignIn"></div>
    <p>The time now is: <%= new Date() %></p>
    <div class="data">
        <p>Profile Detail</p>
        <img id="pic" class="img-circle" width="100" height="100">
        <p>Email Address0</p>
        <p id="email" class="alert alert-danger"></p>
        <button onclick="onSignOut();" class="btn btn-danger">Signout</button>
    </div>
</body>

</html>