requirejs.config({
    baseUrl: '/js/library',
    paths: {
        bootstrap: 'bootstrap.min',
        jquery: 'jquery-3.3.0.min',
        api_url: 'apiurl',
        OAuth: 'googleAuth',
        backbone:'backbone-min'
    },
    waitSeconds: 200
});

